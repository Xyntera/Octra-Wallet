name: Build & Release Octra Wallet

on:
  push:
    branches: [main, master]
    tags:
      - 'v*'
  pull_request:
    branches: [main, master]
  workflow_dispatch:

jobs:
  build-android:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
      
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.0'
          channel: 'stable'
      
      - name: Get Dependencies
        run: flutter pub get
      
      - name: Build APK
        run: flutter build apk --release
      
      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: OctraWallet-Android
          path: build/app/outputs/flutter-apk/app-release.apk

  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.0'
          channel: 'stable'
      
      - name: Enable Windows Desktop
        run: flutter config --enable-windows-desktop
      
      - name: Get Dependencies
        run: flutter pub get
      
      - name: Build Windows
        run: flutter build windows --release
      
      - name: Create MSIX Installer
        run: dart run msix:create
      
      - name: Upload Windows Installer
        uses: actions/upload-artifact@v4
        with:
          name: OctraWallet-Windows
          path: build/windows/x64/runner/Release/*.msix

  release:
    needs: [build-android, build-windows]
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      
      - name: Download Android APK
        uses: actions/download-artifact@v4
        with:
          name: OctraWallet-Android
          path: ./release
      
      - name: Download Windows Build
        uses: actions/download-artifact@v4
        with:
          name: OctraWallet-Windows
          path: ./release
      
      - name: Get Version
        id: version
        run: echo "version=$(date +'%Y.%m.%d-%H%M')" >> $GITHUB_OUTPUT
      
      - name: Rename Files
        run: |
          mv ./release/app-release.apk ./release/OctraWallet-${{ steps.version.outputs.version }}.apk
          for f in ./release/*.msix; do mv "$f" "./release/OctraWallet-${{ steps.version.outputs.version }}.msix"; done
      
      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ steps.version.outputs.version }}
          name: Octra Wallet v${{ steps.version.outputs.version }}
          body: |
            ## 🦉 Octra Wallet Release
            
            **Build Date:** ${{ steps.version.outputs.version }}
            
            ### Downloads
            - **Android**: `OctraWallet-${{ steps.version.outputs.version }}.apk`
            - **Windows Installer**: `OctraWallet-${{ steps.version.outputs.version }}.msix` (installs to Start Menu)
            
            ### Features
            - Security PIN & Biometrics (required)
            - Multi-wallet support
            - Encrypted/Private transactions
            - Owl Logo animation
          draft: false
          prerelease: false
          files: |
            ./release/OctraWallet-${{ steps.version.outputs.version }}.apk
            ./release/OctraWallet-${{ steps.version.outputs.version }}.msix
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
