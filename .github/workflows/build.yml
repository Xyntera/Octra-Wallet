name: Build & Release Octra Wallet

on:
  push:
    branches: [main, master]
    tags:
      - 'v*'
  pull_request:
    branches: [main, master]
  workflow_dispatch:

jobs:
  build-android:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
      
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.0'
          channel: 'stable'
      
      - name: Get Dependencies
        run: flutter pub get
      
      - name: Build APK
        run: flutter build apk --release
      
      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: OctraWallet-Android
          path: build/app/outputs/flutter-apk/app-release.apk

  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.0'
          channel: 'stable'
      
      - name: Enable Windows Desktop
        run: flutter config --enable-windows-desktop
      
      - name: Get Dependencies
        run: flutter pub get
      
      - name: Build Windows
        run: flutter build windows --release
      
      - name: Create Windows ZIP
        run: Compress-Archive -Path build/windows/x64/runner/Release/* -DestinationPath OctraWallet-Windows.zip
      
      - name: Upload Windows Build
        uses: actions/upload-artifact@v4
        with:
          name: OctraWallet-Windows
          path: OctraWallet-Windows.zip

  release:
    needs: [build-android, build-windows]
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      
      - name: Download Android APK
        uses: actions/download-artifact@v4
        with:
          name: OctraWallet-Android
          path: ./release
      
      - name: Download Windows Build
        uses: actions/download-artifact@v4
        with:
          name: OctraWallet-Windows
          path: ./release
      
      - name: Get Version
        id: version
        run: echo "version=$(date +'%Y.%m.%d-%H%M')" >> $GITHUB_OUTPUT
      
      - name: Rename Files
        run: |
          mv ./release/app-release.apk ./release/OctraWallet-${{ steps.version.outputs.version }}.apk
          mv ./release/OctraWallet-Windows.zip ./release/OctraWallet-Windows-${{ steps.version.outputs.version }}.zip
      
      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ steps.version.outputs.version }}
          name: Octra Wallet v${{ steps.version.outputs.version }}
          body: |
            ## 🦉 Octra Wallet Release
            
            **Build Date:** ${{ steps.version.outputs.version }}
            
            ### Downloads
            - **Android APK**: `OctraWallet-${{ steps.version.outputs.version }}.apk`
            - **Windows**: `OctraWallet-Windows-${{ steps.version.outputs.version }}.zip` (extract and run `octra_wallet.exe`)
            
            ### Features
            - 🔒 Security PIN & Biometrics (required)
            - 🎬 Animated Owl Logo (video splash)
            - 💰 Multi-wallet support
            - 🔐 Encrypted/Private transactions
          draft: false
          prerelease: false
          files: |
            ./release/OctraWallet-${{ steps.version.outputs.version }}.apk
            ./release/OctraWallet-Windows-${{ steps.version.outputs.version }}.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
